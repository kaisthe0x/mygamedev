[gd_scene load_steps=4 format=3]

; Come Closer special -- the magnet FIELD. Its script (magnet_field.gd) grabs nearby enemies + stuns
; them on arrival (tune the pull there). The particles are a placeholder inward "pull" swirl -- reskin
; the texture / gradient / shape below. The Strike-less pull is all in the script; this is pure visual.

[ext_resource type="Script" path="res://scripts/combat/magnet_field.gd" id="1_field"]
[ext_resource type="Texture2D" path="res://vfx/shared/textures/pixel_ember.png" id="2_tex"]

[sub_resource type="Gradient" id="grad"]
offsets = PackedFloat32Array(0, 1)
colors = PackedColorArray(0.75, 0.45, 1.6, 0.9, 0.35, 0.1, 0.7, 0)

[node name="ComeCloserField" type="Node2D"]
script = ExtResource("1_field")

[node name="Particles" type="CPUParticles2D" parent="."]
texture = ExtResource("2_tex")
amount = 36
lifetime = 0.5
one_shot = true
explosiveness = 0.4
emission_shape = 1
emission_sphere_radius = 130.0
direction = Vector2(0, 0)
spread = 180.0
gravity = Vector2(0, 0)
initial_velocity_min = 0.0
initial_velocity_max = 20.0
radial_accel_min = -620.0
radial_accel_max = -420.0
scale_amount_min = 0.15
scale_amount_max = 0.3
color_ramp = SubResource("grad")
